# Raw Dataset

This folder contains the bundled Walmart weekly sales dataset sample used by the dashboard.

Main dataset file:

```text
data/raw/walmart_real_sales.csv
```

The bundled CSV is a small sample from the public Walmart weekly sales dataset. To refresh it with the full dataset, run:

```bash
python -m src.download_dataset
```
